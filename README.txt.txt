Academic project for Data Structures course, Spring 2013.

Program makes use of a binary search tree and a heuristic to quickly and accurately determine the strings which can be made from a text file representing a Boggle board which is read in on the command line. Uses a dictionary text file to dtermine which strings are actually words.

java Boggle 5x5-input.txt